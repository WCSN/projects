Файлы в каталоге template заполнять текстом в кодировке BASE64 или utf-8

BASE64 предпочтительнее - ганантируется правильная перекодировка на различных серверах.
utf-8 теоретически тоже, но бывает? что не все сервера всё ещё её понимают.
